# 部署指南

## 先看这里：你真的需要后端吗？

「在线实时抓取」需要一台服务器一直跑着，免费套餐普遍有休眠、限速或者要绑卡。
如果你只是想**看某几个人的成绩**，有一个完全免费、零维护、没有冷启动的做法：

```bash
node tools/export-static.mjs          # 把结果固化成数据文件
git add -A && git commit -m "data" && git push
```

推完之后 Pages 上的网站直接就能看：完整图表、逐场明细、CSV 导出全都有，
**不需要任何后端，不需要注册任何平台，也不会休眠**。
唯一的区别是数据是快照 —— 想更新就再跑一次。

> 仓库里已经内置了两份数据（`TYOI · 2026` 和 `TYOI · 全部年份`），
> 所以网站推上去就能直接打开看。
>
> 要分析别的用户：本地跑 `node oj-user.mjs <uid> --json`，把生成的 JSON **直接拖进网页**即可。

---

## 需要真正的在线实时抓取？

那就往下走。整套用免费额度，不需要买服务器和域名。

```
GitHub 仓库（代码 + Pages 前端）  ←→  Render（后端，负责抓取）
```

> 为什么必须有后端？因为 `oj.33dai.cn` **不返回任何 CORS 头**，浏览器不允许网页直接跨域抓它的数据。
> 所以前端可以放 Pages，但抓取必须由服务端来做。

---

## 第 1 步：推送到 GitHub

在仓库目录里双击 **`推送-GitHub.cmd`**，按提示粘贴一个 GitHub Token。

Token 在 https://github.com/settings/tokens 生成：

| 类型 | 需要的权限 |
|---|---|
| 经典 Token | 勾选 **`repo`** 即可（脚本还要开 Pages，建议再勾 `workflow`） |
| 细粒度 Token | `Contents: Read and write`、`Administration: Read and write`、`Pages: Read and write` |

脚本会自动完成：校验 Token → 创建仓库 → 推送 → 配置 `origin` → 开启 Pages。

推完后：

- 仓库：`https://github.com/WangNoNo666/33oj-score-analyzer`
- 前端：`https://wangnono666.github.io/33oj-score-analyzer/`
  （Pages 首次构建约 1 分钟，在仓库 **Actions** 页能看到进度）

> 也可以不用脚本：自己在 GitHub 建好空仓库，然后
> `git remote add origin https://github.com/<你>/<仓库>.git && git push -u origin main`，
> 再去 **Settings → Pages → Source** 选 **GitHub Actions**。

---

## 第 2 步：在 Render 部署后端

1. 打开 https://render.com ，用 GitHub 账号登录；
2. **New → Blueprint**；
3. 选中 `33oj-score-analyzer` 仓库 —— Render 会自动读取仓库根目录的 `render.yaml`；
4. 点 **Apply / Deploy**，等 2~3 分钟。

`render.yaml` 里已经配好了：

```yaml
runtime: docker
plan: free
healthCheckPath: /api/status
envVars:
  - key: OJ_MODE          # 在线模式：用户在页面上填自己的 Cookie
    value: cloud
  - key: OJ_ALLOW_ORIGIN  # 允许 GitHub Pages 跨域调用
    value: https://wangnono666.github.io
```

部署完成后 Render 会给你一个地址，形如：

```
https://33oj-score-analyzer.onrender.com
```

**如果你的仓库名或 GitHub 用户名不一样**，记得回 `render.yaml` 改 `OJ_ALLOW_ORIGIN`，
改成 `https://<你的用户名>.github.io`（**只到域名，不带仓库路径**）。

---

## 第 3 步：把前后端接起来

打开 Pages 站点：

```
https://wangnono666.github.io/33oj-score-analyzer/
```

在「后端已经部署好了？」那一栏填入你的 Render 地址，点 **打开分析器**。
地址会存在浏览器本地，以后直接访问 `/app/` 也行。

进去之后：

1. 在「会话」卡片里粘贴你的 OJ Cookie（界面上有「怎么拿到自己的 Cookie？」的步骤说明）；
2. 填 UID、勾选年份、点「开始分析」。

---

## 常见问题

**首次打开很慢 / 转圈很久**
Render 免费套餐闲置 15 分钟后会休眠，下次请求需要 30~60 秒唤醒。等一会儿就好。

**提示「Cookie 无效或已过期」**
重新登录 `oj.33dai.cn`，再复制一次 `sid` 和 `sid.sig`。
只复制一个是不行的，两个都要。

**抓取时提示「N 场成绩表拿不到」**
多数是被 OJ 限流（短时间内请求太多），或那场比赛还没结束 / 成绩表没公开。
降低「并发数」（建议 3~5）再跑一次。

**跨域被拦（浏览器控制台报 CORS）**
说明 `OJ_ALLOW_ORIGIN` 和实际前端地址不一致。
注意只填到域名：`https://yourname.github.io`，**不要**带 `/33oj-score-analyzer`。

**想用自己的域名**
在 Render 里绑自定义域名，然后把该域名加进 `OJ_ALLOW_ORIGIN`（多个用逗号分隔）。

**不想用 Render**
仓库里还有 `fly.toml`（Fly.io）和 `railway.json`（Railway），以及通用的 `Dockerfile`，
任何支持 Docker 的平台都能跑。关键就三个环境变量：`OJ_MODE=cloud`、`PORT`、`OJ_ALLOW_ORIGIN`。

---

## 安全提醒

在线模式下，服务端**不保存任何凭据**：拿你提供的 Cookie 去请求 OJ，请求结束即丢弃，
不写磁盘、不记日志（异常信息里的 Cookie 会被正则打码）；产物写在系统临时目录，2 小时后自动删除。
页面侧只存在 `sessionStorage`，关掉标签页就没了。

但 Cookie 等同于登录凭证，**填给任何第三方服务前都该想清楚**。
如果你在意，更稳妥的做法是本地运行（见 README 的「快速开始」）——
本机模式从你已登录的 Edge 自动取会话，凭据不出本机。
